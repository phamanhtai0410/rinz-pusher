[unix_http_server]
file=/run/supervisord.sock   ; (the path to the socket file)

[supervisord]
logfile=/var/log/supervisord.log ; (main log file;default $CWD/supervisord.log)
loglevel=info                ; (log level;default info; others: debug,warn,trace)

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix:///run/supervisord.sock ; use a unix:// URL  for a unix socket

[program:the-cua-tui-service]
command=gunicorn -b 0.0.0.0:5005 --workers=2 manage:app --access-logfile='-' --worker-class gevent --log-level=INFO
directory=/webapps/the-cua-tui-service
autostart=true
autorestart=true
redirect_stderr=true
stdout_logfile=/var/log/apps/the_cua_tui_service.log

# Comment if you dont need celery
[program:the_cua_tui_service_celery]
command=celery --app src.tasks worker -Q the_cua_tui_service -l DEBUG -c 4
directory=/webapps/the-cua-tui-service
autostart=true
autorestart=true
redirect_stderr=true
stdout_logfile=/var/log/apps/the_cua_tui_service_celery.log
logfile_maxbytes=10MB
logfile_backups=1
